$("#collect-<%= @post.id %>").parent().prepend("<%= j(link_to "Uncollect", post_uncollect_path(@post), remote: true, method: :post, class: "btn btn-outline-secondary float-right margin-10", id: "uncollect-#{@post.id}") %>");
$("#collect-<%= @post.id %>").remove();